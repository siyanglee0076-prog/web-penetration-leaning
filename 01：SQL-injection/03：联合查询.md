# SQL注入——联合查询

## 1. 实验环境

- 靶场：自建本地 CMS 靶场（PHP + MySQL + Apache）
- 工具：Chrome + Burp Suite + HackBar
- 注入类型：联合查询注入（UNION）

## 2. 联合查询是什么

`UNION` 可以把多个 `SELECT` 查询的结果合并到一起。

在 SQL 注入中，可以利用这一点，把自己想查询的内容拼接到原本的查询结果中，从而尝试获取数据库中的其他信息。MySQL 官方文档也说明，`UNION` 会将多个查询块的结果合并成一个结果集。

简单理解：

```sql
SELECT id,username FROM users
UNION
SELECT 1,2;
```

相当于把两个查询的结果放到一起。

------

## 3. 为什么要先判断列数

联合查询有一个关键条件：

> 两边 `SELECT` 的列数需要一致。

例如原查询有 3 列：

```sql
SELECT id,username,password FROM users
```

那么联合查询也需要对应 3 列：

```sql
UNION SELECT 1,2,3
```

如果列数不一致，通常会报错。不同查询中对应位置的数据类型也需要兼容。

------

## 4. 判断列数

我在参数后面尝试：

```sql
ORDER BY 1
ORDER BY 2
ORDER BY 3
```

不断增加数字。

如果：

```sql
ORDER BY 3
```

正常，而：

```sql
ORDER BY 4
```

报错，

说明当前查询结果大概率有 **3 列**。

也可以使用：

```sql
UNION SELECT NULL
UNION SELECT NULL,NULL
UNION SELECT NULL,NULL,NULL
```

逐渐增加列数进行判断。

------

## 5. 判断哪些列可以回显

确定列数后，例如确定为 3 列：

```sql
UNION SELECT 1,2,3
```

观察页面最终显示了哪些数字。

例如页面显示：

```text
2
```

说明第 2 列可能存在回显位置。

之后就可以尝试把想查询的数据放到这个位置。

<img src="C:\Users\27696\AppData\Roaming\Typora\typora-user-images\image-20260921205233045.png" alt="image-20260921205233045" style="zoom:30%;" />

<img src="C:\Users\27696\AppData\Roaming\Typora\typora-user-images\image-20260921205958966.png" alt="image-20260921205958966" style="zoom:30%;" />

<img src="C:\Users\27696\AppData\Roaming\Typora\typora-user-images\image-20260921210509873.png" alt="image-20260921210509873" style="zoom:30%;" />

------

## 6. 联合查询基本思路

我目前理解的流程：

```text
判断是否存在注入
        ↓
判断原查询列数
        ↓
UNION SELECT 对应列数
        ↓
判断页面回显位置
        ↓
把需要查询的数据放到回显位置
        ↓
继续获取数据库相关信息
```

例如：

```sql
UNION SELECT 1,database(),3
```

如果第二列可以回显，就可能看到当前数据库名称。

------

## 7. 当前学习理解

之前学习万能用户名时，重点是：

> 想办法改变原 SQL 的逻辑。

到了联合查询，我理解成：

> 不只是改变原 SQL 的判断条件，而是尝试在原查询后面追加一个自己的 `SELECT`，让数据库把我查询的结果一起返回。

所以联合查询的核心不是死记 Payload，而是：

**列数 → 回显位置 → 查询内容。**

------

## 8. 实验记录

### 测试环境

自建本地 CMS 靶场（PHP + MySQL + Apache）

### 使用工具

Burp Suite + HackBar

### 本次遇到的问题

- 明白列数之后，又需要判断哪个位置能够在页面中回显
- 理解了 `database()` 可以先确定当前使用的数据库，version()可以确定当前版本，再进一步查询数据库中的表等信息

### 最后总结

联合查询注入的关键就是：

```text
列数 → 回显点 → UNION SELECT → 获取数据
```

不是单纯记 Payload，而是根据页面的实际回显情况一步一步判断。