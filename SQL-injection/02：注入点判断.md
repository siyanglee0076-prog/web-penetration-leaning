# SQL注入：注入点判断

## 1. 实验环境

- 靶场：自建本地 CMS 靶场（PHP + MySQL + Apache）
- 工具：Chrome+Burp Suite + HackBar
- 页面：`/cms/admin/login.php`
- 注入点：username

------

## 2. 判断思路

SQL 注入判断的目的，是确认：

> **用户输入是否能够影响后端 SQL 查询。**

基本思路：

```text
找到可控参数
    ↓
输入特殊字符
    ↓
测试真假条件
    ↓
测试时间延迟
    ↓
观察响应差异
```

------

## 3. 单引号测试

先在参数中输入：

```text
'
```

观察：

- 页面是否报错
- 页面内容是否变化
- HTTP 状态码是否变化
- 响应长度是否变化

如果出现异常，说明这个参数值得进一步测试。

------

## 4. 真 / 假条件测试

测试：

```sql
' AND 1=1
```

和：

```sql
' AND 1=2
```

其中：

```text
1=1 → 真
1=2 → 假
```

分别发送请求并比较结果。

如果两个请求的响应存在明显差异，说明输入可能影响了 SQL 查询条件。

### 为什么这样判断？

单独输入 `'` 出现报错，并不能完全证明存在 SQL 注入。

而：

```text
真条件 → 一种结果
假条件 → 另一种结果
```

可以进一步验证输入是否真正影响了 SQL 的逻辑。

------

## 5. 时间延迟测试

如果页面没有明显报错，真假条件也看不到明显区别，可以观察**响应时间**。

MySQL 中可以使用：

```sql
SLEEP(5)
```

进行延时测试。

核心思路：

```text
正常请求
    ↓
记录响应时间
    ↓
加入延时条件
    ↓
再次请求
    ↓
响应明显增加
    ↓
说明数据库可能执行了延时函数
```

进一步可以让 `SLEEP()` 根据某个条件决定是否执行。

也就是说：

```text
条件成立 → 延时
条件不成立 → 不延时
```

通过这种差异，可以在页面没有明显回显的情况下判断 SQL 条件。

> 实验时需要多测试几次，避免把网络波动误认为 SQL 延迟。

------

## 6. 三种判断方式

目前学习到：

### 报错判断

```text
输入特殊字符
    ↓
观察数据库异常
```

### 布尔判断

```text
AND 1=1
AND 1=2
    ↓
比较响应差异
```

### 时间判断

```text
条件成立
    ↓
SLEEP()
    ↓
观察响应延迟
```

可以简单记成：

> **报错看异常，布尔看差异，时间看延迟。**

------

## 7. Burp Suite + HackBar

通过 HackBar 构造测试语句，并结合 Burp Suite 查看实际请求与响应。

重点观察：

- Status Code
- Response Length
- Response 内容
- 错误信息
- Response Time

其中时间型判断尤其要关注响应时间的变化。

------

## 8. 我的理解

一开始我认为 SQL 注入判断主要就是输入：

```text
'
```

看页面是否报错。

后来发现，即使没有明显报错，也可以通过：

```text
真假条件 → 比较响应
时间条件 → 比较响应时间
```

来判断输入是否影响 SQL 执行。

所以 SQL 注入判断的核心不是记住某个 Payload，而是：

> **制造可观察的差异，再通过差异判断用户输入是否影响了 SQL。**

------

## 9. 实验记录

| 测试      | 目的          | 观察            |
| --------- | ------------- | --------------- |
| `'`       | 测试 SQL 异常 | 页面 / 响应变化 |
| `AND 1=1` | 测试真条件    | 记录响应        |
| `AND 1=2` | 测试假条件    | 记录响应        |
| `SLEEP()` | 测试时间延迟  | 记录响应时间    |

------

